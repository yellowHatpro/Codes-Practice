#include <bits/stdc++.h>
using namespace std;

/* Built in Functions map :

begin() = Return an iterator to the first element in the map

size()  = Returns the size of the element in the Map

empty() = Returns the boolean value indicating whether the map is empty .

insert(pair(key,value)) = Adds a new key-value pair to the map.
Alternate way = map_name[key] = value;

find(val) = Gives the iterator to the 
















*/
int main(){


cout<<endl;
	return 0;
}